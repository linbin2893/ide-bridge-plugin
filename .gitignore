# ---- Gradle ----
.gradle/
build/
# wrapper 的 jar 必须入库，否则 clone 下来跑不了 ./gradlew
# 用 ** 而非锚定路径：Gradle 工程已下移到 idea-plugin/，写死 gradle/ 开头会匹配不到
!**/gradle/wrapper/gradle-wrapper.jar

# Kotlin 2.x 增量编译的会话目录，纯本地状态
.kotlin/

# intellijPlatform 2.x 下载并解压的 IDE 发行版与插件产物，体积大且完全可重建
.intellijPlatform/

# ---- Node / pnpm ----
# dsh-plugin 是纯 ESM 且无外部依赖，正常不产生 node_modules；
# 但它与 DSH profile 下那份运行副本同源，那边是 pnpm workspace 成员，
# 万一在此误跑 pnpm install，产物也不该入库
node_modules/
.pnpm-debug.log*

# ---- IntelliJ IDEA ----
# 本项目的 .idea 全部由 Gradle 导入自动生成，重新 Reload 即可重建；
# 其中 workspace.xml 还带本机路径与窗口布局，入库只会制造无谓冲突
.idea/
*.iml
*.ipr
*.iws
out/

# 本机覆盖配置（SDK 路径等），不应共享
local.properties

# ---- OS / 编辑器杂项 ----
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
